version: 2
jobs:
  #job名
  build:
    docker:
      - image: circleci/python:3.6.1
    working_directory: ~/repo

    steps:
      - run:
        name: install django
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install django



